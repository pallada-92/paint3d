import * as React from 'react';
import { storiesOf } from '@storybook/react';

import WebSocketTest from './WebSocketTest';

storiesOf('WebSocketTest', module).add('default', () => <WebSocketTest />);
